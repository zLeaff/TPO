
const inputList = document.getElementsByClassName("form-control")
const alertList = document.getElementsByClassName('form-alert')

for (let i = 0; i < inputList.length; i++) {
  if (!i) {
    style.borderColor = (redColor)
  } else {
    style.borderColor = (orangeColor)
    innerHTML = ""
  }
}
